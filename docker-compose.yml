services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: insight-frontend
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VITE_PORT=5173
    stdin_open: true
    tty: true
